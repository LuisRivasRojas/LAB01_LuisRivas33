# Puerto configurable via variable de entorno SERVER_PORT (default 8082)
server.port=${SERVER_PORT:8082}

spring.application.name=ms-pedidos

# Configuración de PostgreSQL - Neon
spring.datasource.url=jdbc:postgresql://ep-morning-lake-athxmxxi-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_VtUFN9kj2LbE
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Inicializar schema
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# URL del ms-productos configurable via variable de entorno
ms.productos.url=${MS_PRODUCTOS_URL:http://localhost:8081/api/productos}
