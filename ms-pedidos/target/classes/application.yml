server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: ms-pedidos

  datasource:
    url: jdbc:postgresql://ep-morning-lake-athxmxxi-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_VtUFN9kj2LbE
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

ms:
  productos:
    url: ${MS_PRODUCTOS_URL:http://localhost:8081/api/productos}
